/**
 * @file Constants file for controllers
 */

const ERR_MANDATORY_PARAM_MISSING = 'Mandatory parameters are missing';

const COMMON_ERR_MSG_MAP = {
    MANDATORY_PARAMS: ERR_MANDATORY_PARAM_MISSING,
    BAD_REQUEST: 'Bad request',
};

module.exports = {
    COMMON_ERR_MSG_MAP,
};
